/**
 * 语速配置唯一真源（字/秒）
 * 全链路所有"台词时长估算"必须从此处读取，禁止就地硬编码。
 *
 * 口径说明：3.5 字/秒是中文旁白/对白的舒适语速（含自然停顿），
 * 生成侧与校验侧必须使用同一口径，否则台词会被误判超速。
 */
module.exports = {
  RATES: {
    slow: 2.5,     // 慢速：情绪深沉、抒情、老年角色
    normal: 3.5,   // 正常：标准对话/旁白（全链路基准值）
    fast: 4.5,     // 快速：紧张、战斗、年轻角色
    rapid: 6.0     // 急促：喊叫、紧急情况
  },
  NORMAL: 3.5,     // 无情绪信息时的默认基准
  LIMIT: 4.5,      // 分配器允许的极限语速（超过即触发压缩）
  PUNCTUATION_PAUSE: 0.3,  // 标点停顿补偿（秒/个）
  SENTENCE_PAUSE: 0.5,     // 句末停顿补偿（秒/句）
  MAX_DIALOGUE_RATIO: 0.8  // 台词占镜头时长上限（全局已一致，收编到此）
};
